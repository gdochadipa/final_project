<%= form_with(model: inmail, local: true) do |form| %>
  <% if inmail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(inmail.errors.count, "error") %> prohibited this inmail from being saved:</h2>

      <ul>
      <% inmail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_area :title %>
  </div>

  <div class="field">
    <%= form.label :from %>
    <%= form.text_area :from %>
  </div>

  <div class="field">
    <%= form.label :to %>
    <%= form.text_area :to %>
  </div>

  <div class="field">
    <%= form.label :datecoming %>
    <%= form.date_select :datecoming %>
  </div>

  <div class="field">
    <%= form.label :mailtext %>
    <%= form.text_area :mailtext %>
  </div>

  <div class="field">
    <%= form.label :response %>
    <%= form.text_area :response %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
